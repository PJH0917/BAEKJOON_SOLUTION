#include <stdio.h>

int main() {
	int n, arr[31] = { 0 };
	for (int i = 1; i < 29; i++) {
		scanf("%d", &n);
		arr[n] = n;

	}
	for(int j=1;j<31;j++){
		if (arr[j] == 0) {
			printf("%d\n", j);
		}
	}
}
