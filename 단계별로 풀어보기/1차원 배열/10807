#include <stdio.h>
int main() {
	int N,v;
	int n[100];
	int count = 0;
	scanf("%d",&N);
	for (int i = 0; i < N; i++) {
		scanf("%d", &n[i]);		
	}
	scanf("%d", &v);
		for (int i = 0; i < N; i++) {
		if (n[i] == v) {
			count++;
		}
	}	
	printf("%d\n", count);
}
