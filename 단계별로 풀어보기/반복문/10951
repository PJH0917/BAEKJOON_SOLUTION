#include <stdio.h>
int main() {
    int A,B;
    while (scanf("%d %d", &A, &B)!=EOF) {
        
        printf("%d\n", A + B);
    }    
    return 0;
}
//메모리 할당 범위 조건이 제시되었기에 EOF를 넣어야한다 while의 조건문 부분에
