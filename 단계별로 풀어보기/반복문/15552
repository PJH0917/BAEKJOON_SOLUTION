#include <stdio.h>
int main() {
    int N,A,B;
    scanf("%d",&N);
    for (int i = 0; i < N; ++i) {
        scanf("%d %d",&A,&B);
        printf("%d\n",A+B);
    }
    return 0;
}

//c언어의 경우에는 전의 문제와 동일함(printf와 scanf가 이미 충분히 빠르기 때문이다.후에 다른 언어로 작성할 시에는 주의사항에 따라 빠른 시간 내에 되도록 구현하도록 한다.)
참고하면 좋은 글:https://www.acmicpc.net/board/view/22716
