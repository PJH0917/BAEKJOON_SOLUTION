#include <stdio.h>
int main() {
    int X, N, a, b, sum = 0;
    scanf("%d\n",&X);
    scanf("%d\n",&N);
    for (int i = 0; i < N;i++) {
        scanf("%d %d",&a,&b);
        sum += a * b;

    }
    sum == X ? printf("Yes") : printf("No");
    return 0;
}
